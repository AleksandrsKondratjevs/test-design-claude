<?php

/**
 * @category  Scandiweb
 * @copyright Copyright (c) 2025 Scandiweb, Inc (https://scandiweb.com)
 * @license   http://opensource.org/licenses/OSL-3.0 The Open Software License 3.0 (OSL-3.0)
 */
?>
<?php
/**
 * Blog sidebar posts list template
 *
 * @var $block \Magefan\Blog\Block\Post\PostList\AbstractList
 */
?>

<?php
$_postCollection = $block->getPostCollection();
$width = (int)$block->getBlockImageWidth() ?: 300;
$height = (int)$block->getBlockImageheight() ?: 200;
$imageHelper = $this->helper(\Magefan\Blog\Helper\Image::class);
?>
<?php if ($_postCollection->count()) { ?>
    <div class="block-<?= $escaper->escapeHtml(str_replace('_', '-', $block->getWidgetKey())) ?> block-list-posts mb-12">
        <div class="text-h3-sm lg:text-h4 mb-4 md:mb-6">
            <?= $block->escapeHtml(__('Recent Posts')) ?>
        </div>

        <div class="block-content flex flex-col border-2 border-b-0 border-container-darker">
            <?php foreach ($_postCollection as $_post) {
                $_postUrl = $escaper->escapeUrl($_post->getPostUrl());
                $_postName = $escaper->escapeHtml($_post->getTitle());
            ?>
                <div class="item flex border-b-2 border-container-darker">
                    <?php if ($block->getDisplayImage()) { ?>
                        <?php
                        $featuredImage = $_post->getFeaturedListImage() ?: $_post->getFeaturedImage();
                        $featuredImgAlt = $_post->getData('featured_list_img_alt') ?: $_post->getData('featured_img_alt');
                        $featuredImgAlt = $featuredImgAlt ?: $_postName;
                        ?>
                        <div class="post-image self-start">
                            <a class="post-item-link block size-24" title="<?= $_postName ?>" href="<?= $_postUrl ?>">
                                <?php
                                $featuredImageUrl = $featuredImage
                                    ? $imageHelper->init($_post->getFeaturedListImg() ?: $_post->getFeaturedImg())->resize($width, $height)
                                    : $this->getViewFileUrl('Magefan_Blog::images/default-no-image.png');
                                ?>
                                <img class="h-full w-full object-cover" src="<?= $escaper->escapeUrl($featuredImageUrl) ?>"
                                    alt="<?= $escaper->escapeHtml($featuredImgAlt) ?>" />
                            </a>
                        </div>
                    <?php } ?>

                    <div class="post-info flex-grow-1 p-3 w-full">
                        <a class="post-item-link text-base no-underline text-black font-bold " title="<?= $_postName ?>" href="<?= $_postUrl ?>"><?= $_postName ?></a>

                        <?php if ($block->getDisplayImage() && $_post->isPublishDateEnabled()) { ?>
                            <div class="post-item-date text-xs text-gray-400 mt-2   ">
                                <span class="value"><?= $escaper->escapeHtml($_post->getPublishDate()) ?></span>
                            </div>
                        <?php } ?>
                    </div>
                </div>
            <?php } ?>
        </div>
    </div>

<?php } ?>