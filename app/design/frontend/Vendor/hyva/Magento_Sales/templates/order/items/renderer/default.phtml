<?php
/**
 * @category  Scandiweb
 * @copyright Copyright (c) 2025 Scandiweb, Inc (https://scandiweb.com)
 * @license   http://opensource.org/licenses/OSL-3.0 The Open Software License 3.0 (OSL-3.0)
 */

use Hyva\Theme\Model\LocaleFormatter;
use Magento\Framework\Escaper;
use Magento\Sales\Block\Order\Item\Renderer\DefaultRenderer;

/** @var DefaultRenderer $block */
/** @var Escaper $escaper */
/** @var LocaleFormatter $localeFormatter */

$item = $block->getItem();
$cols = $block->getData('is_context_shipment') ? 3 : ($block->getData('is_context_creditmemo') ? 7 : 5);
?>
<div class="flex flex-col gap-1.5 lg:gap-4 lg:grid grid-cols-<?= $escaper->escapeHtmlAttr($cols) ?>">
    <div class="col-span-2 flex flex-col gap-0.5">
        <span class="font-bold"><?= $escaper->escapeHtml($item->getName()) ?></span>
        <div class="item-options flex flex-col gap-0.5">
            <div class="text-sm flex">
                <span><?= $escaper->escapeHtml(__('SKU')) ?>:</span>
                <span class="text-sm ms-1"><?= /* @noEscape */ $block->prepareSku($block->getSku()) ?></span>
            </div>
            <?php if ($options = $block->getItemOptions()): ?>
                <?php foreach ($options as $option): ?>
                    <div class="text-sm flex">
                        <span><?= $escaper->escapeHtml($option['label']) ?>:</span>
                        <?php if (!$block->getPrintStatus()): ?>
                            <?php $formatedOptionValue = $block->getFormatedOptionValue($option) ?>
                            <span class="ms-1">
                                <?= $escaper->escapeHtml($formatedOptionValue['value'], ['a']) ?>
                            </span>
                        <?php else: ?>
                            <span class="ms-1">
                                <?= $escaper->escapeHtml(
                                    (isset($option['print_value']) ? $option['print_value'] : $option['value'])
                                ) ?>
                            </span>
                        <?php endif; ?>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
            <?php /* downloadable */ ?>
            <?php if ($links = $block->getLinks()): ?>
                <div class="item options">
                    <p><?= $escaper->escapeHtml($block->getLinksTitle()) ?></p>
                    <?php foreach ($links->getPurchasedItems() as $link): ?>
                        <p class="ms-1"><?= $escaper->escapeHtml($link->getLinkTitle()) ?></p>
                    <?php endforeach; ?>
                </div>
            <?php endif; ?>
            <?php $addtInfoBlock = $block->getProductAdditionalInformationBlock(); ?>
            <?php if ($addtInfoBlock): ?>
                <?= $addtInfoBlock->setItem($block->getOrderItem())->toHtml() ?>
            <?php endif; ?>
            <?= $escaper->escapeHtml($item->getDescription()) ?>
        </div>
    </div>
    <?php if (!$block->getData('is_context_shipment')): ?>
        <div class="flex justify-between lg:block">
            <p class="lg:sr-only font-bold"><?= $escaper->escapeHtml(__('Price')) ?></p>
            <?= $block->getItemPriceHtml() ?>
        </div>
    <?php endif; ?>
    <div class="flex justify-between lg:block">
        <p class="lg:sr-only font-bold">
            <?php if ($block->getData('is_context_invoice')): ?>
                <?= $escaper->escapeHtml(__('Qty Invoiced')) ?>
            <?php elseif ($block->getData('is_context_shipment')): ?>
                <?= $escaper->escapeHtml(__('Qty Shipped')) ?>
            <?php else: ?>
                <?= $escaper->escapeHtml(__('Qty')) ?>
            <?php endif; ?>
        </p>
        <div class="text-right lg:text-left">
            <?php if ($block->getData('is_context_invoice') || $block->getData('is_context_creditmemo')): ?>
                <span class="content"><?= /** @noEscape */ $localeFormatter->formatNumber($block->getItem()->getQty()) ?></span>
            <?php endif; ?>
            <?php if ($block->getData('is_context_shipment')): ?>
                <span class="content"><?= /** @noEscape */ $localeFormatter->formatNumber($block->getItem()->getQty()) ?></span>
            <?php endif; ?>
            <?php if ($block->getData('is_context_order')): ?>
                <?php if ($block->getItem()->getQtyOrdered() > 0): ?>
                    <p>
                        <span class="title"><?= $escaper->escapeHtml(__('Ordered')) ?>:</span>
                        <span class="content font-bold"><?= /** @noEscape */ $localeFormatter->formatNumber((float) $block->getItem()->getQtyOrdered()) ?></span>
                    </p>
                <?php endif; ?>
                <?php if ($block->getItem()->getQtyShipped() > 0): ?>
                    <p>
                        <span class="title"><?= $escaper->escapeHtml(__('Shipped')) ?>:</span>
                        <span class="content font-bold"><?= /** @noEscape */ $localeFormatter->formatNumber($block->getItem()->getQtyShipped()) ?></span>
                    </p>
                <?php endif; ?>
                <?php if ($block->getItem()->getQtyCanceled() > 0): ?>
                    <p>
                        <span class="title"><?= $escaper->escapeHtml(__('Canceled')) ?>:</span>
                        <span class="content font-bold"><?= /** @noEscape */ $localeFormatter->formatNumber($block->getItem()->getQtyCanceled()) ?></span>
                    </p>
                <?php endif; ?>
                <?php if ($block->getItem()->getQtyRefunded() > 0): ?>
                    <p>
                        <span class="title"><?= $escaper->escapeHtml(__('Refunded')) ?>:</span>
                        <span class="content font-bold"><?= /** @noEscape */ $localeFormatter->formatNumber($block->getItem()->getQtyRefunded()) ?></span>
                    </p>
                <?php endif; ?>
            <?php endif; ?>
        </div>
    </div>
    <?php if (!$block->getData('is_context_shipment')): ?>
        <div class="flex justify-between lg:block lg:text-right">
            <p class="lg:sr-only font-bold"><?= $escaper->escapeHtml(__('Subtotal')) ?></p>
            <?= $block->getItemRowTotalHtml() ?>
        </div>
    <?php endif; ?>
    <?php if ($block->getData('is_context_creditmemo')): ?>
        <div class="text-right flex justify-between lg:block">
            <p class="lg:sr-only font-bold"><?= $escaper->escapeHtml(__('Discount')) ?></p>
            <?= /* @noEscape */ $block->getOrder()->formatPrice($item->getDiscountAmount()) ?>
        </div>
        <div class="text-right flex justify-between lg:block">
            <p class="lg:sr-only font-bold"><?= $escaper->escapeHtml(__('Row Total')) ?></p>
            <?= $block->getItemRowTotalAfterDiscountHtml() ?>
        </div>
    <?php endif; ?>
</div>
